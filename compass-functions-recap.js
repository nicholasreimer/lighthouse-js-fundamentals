const sayHello = function(name) {
  //function has been declared using a constant var named sayHello 
  //with a var named name as its parameter
  console.log('Hello, ' + name);
  //function body contains a console log statement that prints 
  //the string 'hello, ' + the name var
}

sayHello(Caliban);
//we call the sayHello function and it returns the console log statement as its ouptut